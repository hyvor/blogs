This builds locally and runs an instance that works with hyvor/dev Traefik setup. Visit https://blogs.build.hyvor.localhost to test.

```bash
docker compose -f meta/build.local/compose.build.yaml up --build
```
