version: "3.3"
services:
    hb-mysql:
        image: "mysql:latest"
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: ""
            MYSQL_ROOT_HOST: "%"
            MYSQL_ALLOW_EMPTY_PASSWORD: 1
            MYSQL_DATABASE: hyvor_blogs
        expose:
            - 3306
        volumes:
            - hb-mysql-data:/var/lib/mysql

    hb-meilisearch:
        image: "getmeili/meilisearch:latest"
        expose:
            - "7700"
        volumes:
            - hb-meilisearch-data:/data.ms
        environment:
            MEILI_MASTER_KEY: "MASTER_KEY"

    hb-app:
        image: "hyvor/blogs:latest"
        ports:
            - "127.0.0.1:5000:80"
        env_file:
            - .env.hb
        depends_on:
            - hb-mysql
            - hb-meilisearch

volumes:
    hb-mysql-data:
    hb-meilisearch-data:
