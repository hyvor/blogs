post:
    match: /{slug}
    template: post
    posts_filter: tag.slug = 'blog'
    content_type: text/html
    is_enabled: true

podcast:
    match: /podcast
    template: podcast
    posts_filter: tag.slug = 'podcast'
    content_type: text/html
    is_enabled: true